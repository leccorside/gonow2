import { StyleSheet } from 'react-native';

/**
 * Helper para criar estilos que usam tema dinamicamente
 * Usado internamente para garantir consistência
 */
export const createThemedStyles = StyleSheet.create;
